# dati generati a runtime: restano sul NAS, non nel repo
data/
cache/
logs/
__pycache__/
*.pyc

# config reale: contiene l'hash della password
config/config.yaml

# local deployment settings: host, paths
deploy.env

# optional extras installed on the host (pip install --target lib)
lib/

# strumento locale di manutenzione libreria, non fa parte del server
optimize_comics.py
